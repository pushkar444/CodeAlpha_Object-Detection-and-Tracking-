# Live Object Detection and Tracking - Web Frontend

This is the same frontend served from `docs/` by the Flask app. It runs
TensorFlow.js COCO-SSD in the browser so the free Render deployment stays
lightweight and usable.
